package thomasb.race.engine;

/**
 * A {@code SectionType} describes the properties of a section of the race track.
 */
public interface SectionType {
	
	/**
	 * Returns the maximum speed in the section.
	 * 
	 * @return the maximum speed in the section
	 */
	int getMaxSpeed();
	
}
